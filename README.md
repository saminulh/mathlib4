# Proving the CLT using the Lindenberg Method

## Todo (for proving sum of Gaussians is Gaussian)
- Define convolution of measures (in what generality? Additive Groups?) 
- Prove that if two measures have densities w.r.t the same base measure than the density of their convolution is the convolution of their densities
- Prove that if X and Y are independent random variables then the distribution of X + Y is given by the convolution of X + Y
- Prove that if X and Y are independent random variables whose distributions have densities w.r.t the same base measure than the density of X + Y is given by the convolution of their densities.
